---
last_id: 7
---

# Roadmap issue ledger

This ledger reserves every project-scoped roadmap issue number through `007`. Allocate the next work item as one greater than `last_id`; never lower this value or reuse an issued number after a record is pruned.
